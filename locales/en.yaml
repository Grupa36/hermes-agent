# Hermes static-message catalog -- English (baseline / source of truth)
#
# Only user-facing static messages from the CLI approval prompt and a handful
# of gateway slash-command replies live here.  Agent-generated output, log
# lines, error tracebacks, tool outputs, and slash-command descriptions stay
# in English and are NOT translated -- see agent/i18n.py for scope rationale.
#
# Keys are dotted paths; nesting below is purely for readability.  Values may
# contain {placeholder} tokens for str.format substitution.  When adding a
# new key, add it to EVERY locale file (en/zh/ja/de/es/fr/tr/uk) in the same commit --
# tests/agent/test_i18n.py asserts catalog parity.

approval:
  # CLI approval prompt -- shown when a dangerous command needs user review.
  dangerous_header: "⚠️  DANGEROUS COMMAND: {description}"
  choose_long:     "      [o]nce  |  [s]ession  |  [a]lways  |  [d]eny"
  choose_short:    "      [o]nce  |  [s]ession  |  [d]eny"
  prompt_long:     "      Choice [o/s/a/D]: "
  prompt_short:    "      Choice [o/s/D]: "
  choose_smart_deny: "      [o]nce  |  [d]eny"
  prompt_smart_deny: "      Choice [o/D]: "
  smart_deny_once_inputs: "o,once"
  smart_deny_deny_inputs: "d,deny"
  timeout:         "      ⏱ No answer in {waited} — the command was not run. Ask again to retry, or raise the limit: hermes config set approvals.timeout {suggested}"
  allowed_once:    "      ✓ Allowed once"
  allowed_session: "      ✓ Allowed for this session"
  allowed_always:  "      ✓ Added to permanent allowlist"
  denied:          "      ✗ Denied"
  cancelled:       "      ✗ Cancelled"
  blocklist_message: "This command is on the unconditional blocklist and cannot be approved."

gateway:
  # Messenger replies to slash commands and implicit state changes.
  approval_expired: "⚠️ Approval expired (agent is no longer waiting). Ask the agent to try again."
  draining:         "⏳ Draining {count} active agent(s) before restart..."
  goal_cleared:     "✓ Goal cleared."
  no_active_goal:   "No active goal."
  config_read_failed: "⚠️ Could not read config.yaml: {error}"
  config_save_failed: "⚠️ Could not save config: {error}"

  # /model command output -- shown after a model switch or when listing models.
  # Provider names, model IDs, capability strings, and cost figures are NOT
  # translated -- they're identifiers/values, not prose.  Only the labels
  # ("Provider:", "Context:", etc.) and the help/footer lines are localized.
  model:
    error_prefix:           "Error: {error}"
    switched:               "Model switched to `{model}`"
    provider_label:         "Provider: {provider}"
    context_label:          "Context: {tokens} tokens"
    max_output_label:       "Max output: {tokens} tokens"
    cost_label:             "Cost: {cost}"
    capabilities_label:     "Capabilities: {capabilities}"
    prompt_caching_enabled: "Prompt caching: enabled"
    warning_prefix:         "Warning: {warning}"
    saved_global:           "Saved to config.yaml (`--global`)"
    session_only_hint:      "_(session only — add `--global` to persist)_"
    current_label:          "Current: `{model}` on {provider}"
    current_tag:            " (current)"
    more_models_suffix:     " (+{count} more)"
    usage_switch_model:     "`/model <name>` — switch model"
    usage_switch_provider:  "`/model <name> --provider <slug>` — switch provider"
    usage_persist:          "`/model <name> --global` — persist"

  agents:
    header:                "🤖 **Active Agents & Tasks**"
    active_agents:         "**Active agents:** {count}"
    this_chat:             " · this chat"
    more:                  "... and {count} more"
    running_processes:     "**Running background processes:** {count}"
    async_jobs:            "**Gateway async jobs:** {count}"
    background_delegations: "**Background delegations:** {count}"
    none:                  "No active agents or running tasks."
    state_starting:        "starting"
    state_running:         "running"

  approve:
    no_pending:            "No pending command to approve."
    once_singular:         "✅ Command approved. The agent is resuming..."
    once_plural:           "✅ Commands approved ({count} commands). The agent is resuming..."
    session_singular:      "✅ Command approved (pattern approved for this session). The agent is resuming..."
    session_plural:        "✅ Commands approved (pattern approved for this session) ({count} commands). The agent is resuming..."
    always_singular:       "✅ Command approved (pattern approved permanently). The agent is resuming..."
    always_plural:         "✅ Commands approved (pattern approved permanently) ({count} commands). The agent is resuming..."

  background:
    usage:                 "Usage: /bg <prompt>\nExample: /bg Summarize the top HN stories today\n\nRuns the prompt in a separate session. You can keep chatting — the result will appear here when done."
    started:               "🔄 Background task started: \"{preview}\"\nTask ID: {task_id}\nYou can keep chatting — results will appear when done."

  btw:
    usage:                 "Usage: /btw <question>\nExample: /btw which file was that error in?\n\nAnswers a quick side question about this conversation without interrupting it. For an independent background task, use /bg <prompt>."
    no_history:            "No conversation yet — send your question as a normal message instead."
    no_provider:           "❌ Cannot answer side question: no provider credentials configured."
    started:               "💬 Side question: \"{preview}\"\nAnswering from a snapshot of this conversation — the current work continues."
    answer:                "💬 /btw: \"{preview}\"\n\n{answer}"
    failed:                "❌ /btw failed: \"{preview}\"\n{error}"

  branch:
    db_unavailable:        "Session database not available."
    no_conversation:       "No conversation to branch — send a message first."
    create_failed:         "Failed to create branch: {error}"
    switch_failed:         "Branch created but failed to switch to it."
    branched_one:          "⑂ Branched to **{title}** ({count} message copied)\nOriginal: `{parent}`\nBranch: `{new}`\nUse `/resume` to switch back to the original."
    branched_many:         "⑂ Branched to **{title}** ({count} messages copied)\nOriginal: `{parent}`\nBranch: `{new}`\nUse `/resume` to switch back to the original."
    branched_thread_one:   "⑂ Branched to **{title}** in a new thread ({count} message copied)\nThread: {thread}\nOriginal: `{parent}` — this chat stays on it\nBranch: `{new}`\nUse `/branch --here` to branch in this chat instead."
    branched_thread_many:  "⑂ Branched to **{title}** in a new thread ({count} messages copied)\nThread: {thread}\nOriginal: `{parent}` — this chat stays on it\nBranch: `{new}`\nUse `/branch --here` to branch in this chat instead."
    thread_fallback:       "No thread could be created here, so the branch replaced this chat's session."

  commands:
    usage:                 "Usage: `/commands [page]`"
    skill_header:          "⚡ **Skill Commands**:"
    default_desc:          "Skill command"
    none:                  "No commands available."
    header:                "📚 **Commands** ({total} total, page {page}/{total_pages})"
    nav_prev:              "`/commands {page}` ← prev"
    nav_next:              "next → `/commands {page}`"
    out_of_range:          "_(Requested page {requested} was out of range, showing page {page}.)_"

  compress:
    not_enough:            "Not enough conversation to compress (need at least 4 messages)."
    no_provider:           "No provider configured -- cannot compress."
    nothing_to_do:         "Nothing to compress yet (the transcript is still all protected context)."
    aggressive_unsupported: "--aggressive is not supported; use '/compress here [N]' to keep only recent exchanges, or /undo to drop turns."
    focus_line:            "Focus: \"{topic}\""
    summary_failed:        "⚠️ Summary generation failed ({error}). {count} historical message(s) were removed and replaced with a placeholder; earlier context is no longer recoverable. Consider checking your auxiliary.compression model configuration."
    aborted:               "⚠️ Compression aborted ({error}). No messages were dropped — conversation is unchanged. Run /compress to retry, /reset for a clean session, or check your auxiliary.compression model configuration."
    aux_failed:            "ℹ️ Configured compression model `{model}` failed ({error}). Recovered using your main model — context is intact — but you may want to check `auxiliary.compression.model` in config.yaml."
    turnhold_deferred:     "ℹ️ Context compression deferred — summary still streaming. Continuing without compression this turn."
    failed:                "Compression failed: {error}"

  debug:
    upload_failed:         "✗ Failed to upload debug report: {error}"
    header:                "**Debug report uploaded:**"
    auto_delete:           "⏱ Pastes will auto-delete in 6 hours."
    full_logs_hint:        "For full log uploads, use `hermes debug share` from the CLI."
    share_hint:            "Share these links with the Hermes team for support."

  deny:
    stale:                 "❌ Command denied (approval was stale)."
    no_pending:            "No pending command to deny."
    denied_singular:       "❌ Command denied."
    denied_plural:         "❌ Commands denied ({count} commands)."
    denied_reason_singular: "❌ Command denied. Reason relayed to the agent: \"{reason}\""
    denied_reason_plural:  "❌ Commands denied ({count} commands). Reason relayed to the agent: \"{reason}\""

  fast:
    not_supported:         "⚡ /fast is only available for OpenAI models that support Priority Processing."
    status:                "⚡ Priority Processing\n\nCurrent mode: `{mode}`\n\n_Usage:_ `/fast <normal|fast|auto|cold|status>`"
    unknown_arg:           "⚠️ Unknown argument: `{arg}`\n\n**Valid options:** normal, fast, auto, cold, status"
    saved:                 "⚡ ✓ Priority Processing: **{label}** (saved to config)\n_(takes effect on next message)_"
    session_only:          "⚡ ✓ Priority Processing: **{label}** (this session only)"
    label_fast:            "FAST"
    label_normal:          "NORMAL"
    status_fast:           "fast"
    status_normal:         "normal"
    picker_title:          "⚡ **Priority Processing**\n\nCurrent mode: `{mode}`\n\nPick an option:"
    choice_fast:           "fast — Priority Processing on"
    choice_normal:         "normal — standard processing"
    choice_auto:           "auto — fast for the first seconds of every turn"
    choice_cold:           "cold — fast for the first turn of a session only"

  footer:
    status:                "📎 Runtime footer: **{state}**\nFields: `{fields}`\nPlatform: `{platform}`"
    usage:                 "Usage: `/footer [on|off|status]`"
    saved:                 "📎 Runtime footer: **{state}**{example}\n_(saved globally — takes effect on next message)_"
    example_line:          "\nExample: `{preview}`"
    state_on:              "ON"
    state_off:             "OFF"

  goal:
    unavailable:           "Goals unavailable on this session."
    no_goal_set:           "No goal set."
    paused:                "⏸ Goal paused: {goal}"
    no_resume:             "No goal to resume."
    resumed:               "▶ Goal resumed: {goal}\nContinuing now — I'll take the next step right away."
    invalid:               "Invalid goal: {error}"
    set:                   "⊙ Goal set ({budget}-turn budget): {goal}\nI'll keep working until the goal is done, you pause/clear it, or the budget is exhausted.\nControls: /goal status · /goal pause · /goal resume · /goal clear"

  help:
    header:                "📖 **Hermes Commands**\n"
    skill_header:          "\n⚡ **Skill Commands** ({count} active):"
    more_use_commands:     "\n... and {count} more. Use `/commands` for the full paginated list."

  insights:
    invalid_days:          "Invalid --days value: {value}"
    error:                 "Error generating insights: {error}"

  kanban:
    error_prefix:          "⚠ kanban error: {error}"
    subscribed_suffix:     "(subscribed — you'll be notified when {task_id} completes or blocks)"
    truncated_suffix:      "… (truncated; use `hermes kanban …` in your terminal for full output)"
    no_output:             "(no output)"
    wake:
      completed:           "completed"
      gave_up:             "gave up (retries exhausted)"
      crashed:             "crashed (worker exited); dispatcher will retry"
      timed_out:           "timed out; dispatcher will retry"
      blocked:             "blocked; needs attention"
      review_requested:    "handed off for review; the implementation is done"
      changes_requested:   "review requested changes (BLOCK); implementation is not approved"
      block_loop_detected: "routed to triage after repeated blocks; orchestration attention needed"
      status_default:      "status changed"
      status_joiner:       ", "
      message:             "[kanban] Task {task_id} {status}.\nTitle: {title}\nAssignee: @{assignee}\nBoard: {board}\n\nCheck the result or decide the next step."
      handoff:             "Result: {summary}"
      review_detail:       "Review feedback: {reason}\nInspect the existing card and its current review run; return work to the same implementation task. Do not treat this BLOCK as approval and do not create a duplicate task."
      guidance:            "This is an automatic task-status notification, not a request to decompose the task again. Inspect the current board before creating follow-up tasks; do not recreate tasks or graphs that already exist."

  personality:
    none_configured:       "No personalities configured in `{path}/config.yaml`"
    header:                "🎭 **Available Personalities**\n"
    none_option:           "• `none` — (no personality overlay)"
    item:                  "• `{name}` — {preview}"
    usage:                 "\nUsage: `/personality <name>`"
    save_failed:           "⚠️ Failed to save personality change: {error}"
    cleared:               "🎭 Personality cleared — using base agent behavior.\n_(takes effect on next message)_"
    set_to:                "🎭 Personality set to **{name}**\n_(takes effect on next message)_"
    unknown:               "Unknown personality: `{name}`\n\nAvailable: {available}"

  profile:
    header:                "👤 **Profile:** `{profile}`"
    home:                  "📂 **Home:** `{home}`"

  reasoning:
    level_default:             "medium (default)"
    level_disabled:            "none (disabled)"
    scope_session:             "session override"
    scope_global:              "global config"
    status:                    "🧠 **Reasoning Settings**\n\n**Effort:** `{level}`\n**Scope:** {scope}\n**Display:** {display}\n\n_Usage:_ `/reasoning <none|minimal|low|medium|high|xhigh|max|ultra|reset|show|hide> [--global]`"
    display_on:                "on ✓"
    display_off:               "off"
    display_set_on:            "🧠 ✓ Reasoning display: **ON**\nModel thinking will be shown before each response on **{platform}**."
    display_set_off:           "🧠 ✓ Reasoning display: **OFF** for **{platform}**"
    reset_global_unsupported:  "⚠️ `/reasoning reset --global` is not supported. Use `/reasoning <level> --global` to change the global default."
    reset_done:                "🧠 ✓ Session reasoning override cleared; falling back to global config."
    unknown_arg:               "⚠️ Unknown argument: `{arg}`\n\n**Valid levels:** none, minimal, low, medium, high, xhigh, max, ultra\n**Display:** show, hide\n**Persist:** add `--global` to save beyond this session"
    picker_title:              "🧠 **Reasoning Settings**\n\n**Effort:** `{level}`\n**Scope:** {scope}\n**Display:** {display}\n\nPick an option:"
    choice_none:               "none — disable reasoning"
    choice_reset:              "reset — clear session override"
    choice_show:               "show reasoning in replies"
    choice_hide:               "hide reasoning from replies"
    set_global:                "🧠 ✓ Reasoning effort set to `{effort}` (saved to config)\n_(takes effect on next message)_"
    set_global_save_failed:    "🧠 ✓ Reasoning effort set to `{effort}` (session only — config save failed)\n_(takes effect on next message)_"
    set_session:               "🧠 ✓ Reasoning effort set to `{effort}` (session only — add `--global` to persist)\n_(takes effect on next message)_"

  reload_mcp:
    cancelled:             "🟡 /reload-mcp cancelled. MCP tools unchanged."
    always_followup:       "ℹ️ Future `/reload-mcp` calls will run without confirmation. Re-enable via `approvals.mcp_reload_confirm: true` in config.yaml."
    confirm_prompt:        "⚠️ **Confirm /reload-mcp**\n\nReloading MCP servers rebuilds the tool set for this session and **invalidates the provider prompt cache** — the next message will re-send full input tokens.  On long-context or high-reasoning models this can be expensive.\n\nChoose:\n• **Approve Once** — reload now\n• **Always Approve** — reload now and silence this prompt permanently\n• **Cancel** — leave MCP tools unchanged\n\n_Text fallback: reply `/approve`, `/always`, or `/cancel`._"
    header:                "🔄 **MCP Servers Reloaded**\n"
    reconnected:           "♻️ Reconnected: {names}"
    added:                 "➕ Added: {names}"
    removed:               "➖ Removed: {names}"
    none_connected:        "No MCP servers connected."
    tools_available:       "\n🔧 {tools} tool(s) available from {servers} server(s)"
    failed:                "❌ MCP reload failed: {error}"

  reload_skills:
    header:                "🔄 **Skills Reloaded**\n"
    no_new:                "No new skills detected."
    total:                 "\n📚 {count} skill(s) available"
    added_header:          "➕ **Added Skills:**"
    removed_header:        "➖ **Removed Skills:**"
    item_with_desc:        "    - {name}: {desc}"
    item_no_desc:          "    - {name}"
    failed:                "❌ Skills reload failed: {error}"

  reset:
    header_default:        "✨ Session reset! Starting fresh."
    header_new:            "✨ New session started!"
    header_titled:         "✨ New session started: {title}"
    title_rejected:        "\n⚠️ Title rejected: {error}"
    title_error_untitled:  "\n⚠️ {error} — session started untitled."
    title_empty_untitled:  "\n⚠️ Title is empty after cleanup — session started untitled."
    tip:                   "\n✦ Tip: {tip}"

  restart:
    in_progress:           "⏳ Gateway restart already in progress..."
    restarting:            "♻ Restarting gateway. If you aren't notified within 60 seconds, restart from the console with `hermes gateway restart`."

  resume:
    db_unavailable:        "Session database not available."
    parse_error:           "⚠️ Could not parse `/resume` arguments: {error}.\nUse quotes around titles with spaces, for example: `/resume \"Project A Plan\"`."
    matrix_no_named_sessions: "No named sessions found for this Matrix room.\nUse `/title My Session` to name the current room session, `/resume --all` to list all Matrix sessions, or `/resume --cross-room <session name>` to explicitly cross room boundaries."
    matrix_blocked_no_origin: "⚠️ Matrix /resume blocked: this named session has no recorded room origin, so Hermes will not resume it inside the current room by default. Use `/resume --cross-room {name}` if you intentionally want to cross room boundaries."
    matrix_blocked_other_room: "⚠️ Matrix /resume blocked: that session belongs to a different Matrix room ({room}). Use `/resume --cross-room {name}` if you intentionally want to resume it here."
    matrix_cross_room_success: "⚠️ Cross-room resume: resumed **{title}** inside Matrix room **{room}**.\nFuture messages in this room will use that transcript until `/reset` or another `/resume`.{msg_part}"
    blocked_not_owner:     "⚠️ /resume blocked: '**{name}**' belongs to a different user or chat. You can only resume sessions from this chat."
    no_named_sessions:     "No named sessions found.\nUse `/title My Session` to name your current session, then `/resume My Session` to return to it later."
    all_requires_admin: "_Note: `--all` (cross-chat listing) requires a configured admin; showing this chat's sessions only._"
    list_header:           "📋 **Named Sessions**\n"
    list_item:             "• **{title}**{preview_part}"
    list_item_numbered:    "{index}. **{title}**{preview_part}"
    list_preview_suffix:   " — _{preview}_"
    list_footer:           "\nUsage: `/resume <session name>`"
    list_footer_numbered:  "\nUsage: `/resume <session name>` or `/resume <number>` (e.g. `/resume 1` for the most recent)"
    list_failed:           "Could not list sessions: {error}"
    out_of_range:          "Resume index {index} is out of range.\nUse `/resume` with no arguments to see available sessions."
    not_found:             "No session found matching '**{name}**'.\nUse `/resume` with no arguments to see available sessions."
    already_on:            "📌 Already on session **{name}**."
    switch_failed:         "Failed to switch session."
    resumed_one:           "↻ Resumed session **{title}** ({count} message). Conversation restored."
    resumed_many:          "↻ Resumed session **{title}** ({count} messages). Conversation restored."
    resumed_no_count:      "↻ Resumed session **{title}**. Conversation restored."

  retry:
    no_previous:           "No previous message to retry."

  rollback:
    not_enabled:           "Checkpoints are not enabled.\nEnable in config.yaml:\n```\ncheckpoints:\n  enabled: true\n```"
    none_found:            "No checkpoints found for {cwd}"
    invalid_number:        "Invalid checkpoint number. Use 1-{max}."
    restored:              "✅ Restored to checkpoint {hash}: {reason}\nA pre-rollback snapshot was saved automatically."
    kept_user_edits:       "↷ Kept your hand-edits: {files}\nUse /rollback <N> --all to restore those too."
    kept_oversize:         "↷ Kept (too large for checkpoints, no stored copy to revert to): {files}"
    failed_deletes:        "⚠️ Could not remove (left in place): {files}"
    restore_failed:        "❌ {error}"

  diff:
    not_enabled:           "Checkpoints are not enabled, so there's no session baseline.\nEnable in config.yaml:\n```\ncheckpoints:\n  enabled: true\n```\nPlain /diff still works — it uses git directly."
    no_changes:            "No changes."
    failed:                "{error}"

  set_home:
    save_failed:           "Failed to save home channel: {error}"
    success:               "✅ Home channel set to **{name}** (ID: {chat_id}).\nCron jobs and cross-platform messages will be delivered here."

  context:
    header:                "🧠 **Context Window**"
    model:                 "Model: `{model}`"
    window:                "Window: {total} tokens"
    in_use:                "In use: {used} / {total} ({pct}%)"
    bar:                   "{bar}"
    headroom:              "Headroom to limit: {headroom} tokens"
    threshold:             "Auto-compresses at: {threshold} ({threshold_pct}%) — {to_go} to go"
    over_threshold:        "⚠️ **Over auto-compression threshold ({threshold}, {threshold_pct}%)**"
    compressions:          "Compressions this session: {count}"
    last_savings:          "Last compression freed: {savings}% of context"
    totals_header:         "Session totals (cumulative across {calls} API calls)"
    totals_line:           "Input {input} · Output {output} · Reasoning {reasoning}"
    total_billed:          "Total billed: {total}"
    throughput_note:       "_Throughput, not context size — each call re-sends the window above._"
    estimated:             "Estimated context: ~{count} tokens across {messages} messages"
    detail_after_first:    "_(Full compression and throughput stats available after the first agent response)_"
    no_data:               "No context data available yet. Send a message to start a session."

  status:
    header:                "📊 **Hermes Gateway Status**"
    matrix_scope_header:   "**Matrix scope:**"
    matrix_scope_room:     "  room: {room}"
    matrix_scope_room_id:  "  room_id: {room_id}"
    matrix_scope_thread:   "  thread_id: {thread_id}"
    matrix_scope_mode:     "  session_scope: {scope}"
    matrix_scope_key:      "  session_key: {session_key}"
    session_id:            "**Session ID:** `{session_id}`"
    title:                 "**Title:** {title}"
    created:               "**Created:** {timestamp}"
    last_activity:         "**Last Activity:** {timestamp}"
    model:                 "**Model:** `{model}`"
    model_provider:        "**Model:** `{model}` ({provider})"
    free_tier:             "Nous · free tier · nous/welcome · /login to sign in"
    context:               "**Context:** {used} / {total} ({pct}%)"
    context_used:          "**Context:** ~{used} tokens"
    tokens:                "**Lifetime tokens billed:** {tokens} _(not your current context size; use `/context`)_"
    agent_running:         "**Agent Running:** {state}"
    state_yes:             "Yes ⚡"
    state_no:              "No"
    queued:                "**Queued follow-ups:** {count}"
    platforms:             "**Connected Platforms:** {platforms}"

  stop:
    stopped_pending:       "⚡ Stopped. The agent hadn't started yet — you can continue this session."
    stopped:               "⚡ Stopped. You can continue this session."
    no_active:             "No active task to stop."

  title:
    db_unavailable:        "Session database not available."
    warn_prefix:           "⚠️ {error}"
    empty_after_clean:     "⚠️ Title is empty after cleanup. Please use printable characters."
    set_to:                "✏️ Session title set: **{title}**"
    not_found:             "Session not found in database."
    current_with_title:    "📌 Session: `{session_id}`\nTitle: **{title}**"
    current_no_title:      "📌 Session: `{session_id}`\nNo title set. Usage: `/title My Session Name`"

  topic:
    not_telegram_dm:         "The /topic command is only available in Telegram private chats."
    no_session_db:           "Session database not available."
    unauthorized:            "You are not authorized to use /topic on this bot."
    restore_needs_topic:     "To restore a session, first create or open a Telegram topic, then send /topic <session-id> inside that topic. To create a new topic, open All Messages and send any message there."
    topics_disabled:         "Telegram topics are not enabled for this bot yet.\n\nHow to enable them:\n1. Open @BotFather.\n2. Choose your bot.\n3. Open Bot Settings → Threads Settings.\n4. Turn on Threaded Mode and make sure users are allowed to create new threads.\n\nThen send /topic again."
    topics_user_disallowed:  "Telegram topics are enabled, but users are not allowed to create topics.\n\nOpen @BotFather → choose your bot → Bot Settings → Threads Settings, then turn off 'Disallow users to create new threads'.\n\nThen send /topic again."
    enable_failed:           "Failed to enable Telegram topic mode: {error}"
    bound_status:            "This topic is linked to:\nSession: {label}\nID: {session_id}\n\nUse /new to replace this topic with a fresh session.\nFor parallel work, open All Messages and send a message there to create another topic."
    thread_ready:            "Telegram multi-session topics are enabled.\n\nThis topic will be used as an independent Hermes session. Use /new to replace this topic's current session. For parallel work, open All Messages and send a message there to create another topic."
    untitled_session:        "Untitled session"

  undo:
    nothing:               "Nothing to undo."
    removed:               "↩️ Undid {turns} turn(s) ({count} message(s)).\nBacked up to: \"{preview}\"\nCopy/edit the text above and send it to re-prompt from here."
    invalid_count:         "Invalid count \"{arg}\" — use /undo or /undo N."

  update:
    platform_not_messaging:  "✗ /update is only available from messaging platforms. Run `hermes update` from the terminal."
    not_git_repo:            "✗ Not a git repository — cannot update."
    hermes_cmd_not_found:    "✗ Could not locate the `hermes` command. Hermes is running, but the update command could not find the executable on PATH or via the current Python interpreter. Try running `hermes update` manually in your terminal."
    start_failed:            "✗ Failed to start update: {error}"
    starting:                "☤ Starting Hermes update… I'll stream progress here."

  usage:
    rate_limits:              "⏱️ **Rate Limits:** {state}"
    header_session:           "📊 **Session Token Usage**"
    label_model:              "Model: `{model}`"
    label_input_tokens:       "Input tokens: {count}"
    label_cache_read:         "Cache read tokens: {count}"
    label_cache_write:        "Cache write tokens: {count}"
    label_output_tokens:      "Output tokens: {count}"
    label_total:              "Total: {count}"
    label_api_calls:          "API calls: {count}"
    label_cost:               "Cost: {prefix}${amount}"
    label_cost_included:      "Cost: included"
    label_context:            "Context: {used} / {total} ({pct}%)"
    label_compressions:       "Compressions: {count}"
    breakdown_header:         "🧩 **Context breakdown** _(estimated)_"
    breakdown_line:           "• {label}: ~{count} ({pct}%)"
    breakdown_cat_system_prompt:        "System prompt"
    breakdown_cat_tool_definitions:     "Tool definitions"
    breakdown_cat_rules:                "Rules"
    breakdown_cat_skills:               "Skills"
    breakdown_cat_mcp:                  "MCP"
    breakdown_cat_subagent_definitions: "Subagent definitions"
    breakdown_cat_memory:               "Memory"
    breakdown_cat_conversation:         "Conversation"
    header_session_info:      "📊 **Session Info**"
    label_messages:           "Messages: {count}"
    label_estimated_context:  "Estimated context: ~{count} tokens"
    detailed_after_first:     "_(Detailed usage available after the first agent response)_"
    no_data:                  "No usage data available for this session."
    unknown_subcommand:       "Unknown /usage subcommand: `{args}`. Try `/usage` or `/usage reset [--force]`."
    reset_wrong_provider:     "Banked usage resets are only available on the openai-codex provider. Switch with `/model` first."

  credits:
    not_logged_in:            "Not logged into Nous Portal. Log in to see your credit balance and top up."

  verbose:
    not_enabled:           "The `/verbose` command is not enabled for messaging platforms.\n\nEnable it in `config.yaml`:\n```yaml\ndisplay:\n  tool_progress_command: true\n```"
    mode_off:              "⚙️ Tool progress: **OFF** — no tool activity shown."
    mode_new:              "⚙️ Tool progress: **NEW** — shown when tool changes (preview length: `display.tool_preview_length`, default 40)."
    mode_all:              "⚙️ Tool progress: **ALL** — every tool call shown (preview length: `display.tool_preview_length`, default 40)."
    mode_verbose:          "⚙️ Tool progress: **VERBOSE** — every tool call with full arguments."
    mode_log:              "⚙️ Tool progress: **LOG** — silent in chat; tool calls written to ~/.hermes/logs/tool_calls.log."
    saved_suffix:          "_(saved for **{platform}** — takes effect on next message)_"
    save_failed:           "_(could not save to config: {error})_"

  voice:
    enabled_voice_only:    "Voice mode enabled.\nI'll reply with voice when you send voice messages.\nUse /voice tts to get voice replies for all messages."
    disabled_text:         "Voice mode disabled. Text-only replies."
    tts_enabled:           "Auto-TTS enabled.\nAll replies will include a voice message."
    status_mode:           "Voice mode: {label}"
    status_channel:        "Voice channel: #{channel}"
    status_participants:   "Participants: {count}"
    status_member:         "  - {name}{status}"
    speaking:              " (speaking)"
    enabled_short:         "Voice mode enabled."
    disabled_short:        "Voice mode disabled."
    label_off:             "Off (text only)"
    label_voice_only:      "On (voice reply to voice messages)"
    label_all:             "TTS (voice reply to all messages)"
    help:                  "{toggle}\n\n**How /voice works**\n• `/voice on` — voice reply when you send a voice message\n• `/voice tts` — voice reply to *every* message\n• `/voice off` — back to text-only replies\n• `/voice status` — show the current mode\n• `/voice` (no argument) — quick toggle between on and off{channels}"
    help_channels:         "\n\n**Live voice channels (Discord)**\n• Join a voice channel first, then `/voice channel` — I'll join, listen, and speak my replies\n• `/voice leave` — disconnect from the voice channel"

  yolo:
    disabled:                   "⚠️ YOLO mode **OFF** for this session — dangerous commands will require approval."
    enabled:                    "⚡ YOLO mode **ON** for this session — all commands auto-approved. Use with caution."

  shared:
    session_db_unavailable:      "Session database not available."
    session_db_unavailable_prefix: "Session database not available"
    session_not_found:           "Session not found in database."
    warn_passthrough:            "⚠️ {error}"

# ---------------------------------------------------------------------------
# Grupa36 fork: Slack-visible gateway strings extracted for i18n. These keys exist
# only in en.yaml + pl.yaml (tests/agent/test_i18n.py exempts g36.* elsewhere).
# ---------------------------------------------------------------------------
g36:
  email:
    unknown_thread: "Unknown email thread {chat_id}: no remembered inbound audience"
  run_busy:
    gerund_restarting:          "restarting"
    gerund_shutting_down:       "shutting down"
    drain_queued:               "⏳ Gateway {action} — queued for the next turn after it comes back."
    drain_rejected:             "⏳ Gateway is {action} and is not accepting another turn right now."
    demoted_tail:               " — your message is queued for when it finishes (use /stop to cancel everything)."
    min_elapsed:                "{minutes} min elapsed"
    running_tool:               "running: {tool}"
    ack_steer_subagents:        "⏩ Steered into current run and its active subagent(s)"
    ack_steer_subagents_tail:   ". Your message arrives after their next tool call."
    ack_steer:                  "⏩ Steered into current run"
    ack_steer_tail:             ". Your message arrives after the next tool call."
    ack_redirect:               "↪ Redirected current run"
    ack_redirect_tail:          ". I'll adjust using your correction."
    ack_subagent_working:       "⏳ Subagent working"
    ack_compressing:            "⏳ Compressing context"
    ack_queued:                 "⏳ Queued for the next turn"
    ack_queued_tail:            ". I'll respond once the current task finishes."
    ack_interrupt:              "⚡ Interrupting current task"
    ack_interrupt_tail:         ". I'll respond to your message shortly."
    busy_reject:                "⏳ Agent is running — `/{name}` can't run mid-turn. Wait for the current response or `/stop` first."
    pause_resumed:              "▶️ Resumed — new work is accepted again."
    pause_not_paused:           "Hermes wasn't paused."
    pause_reason:               " (reason: {reason})"
    pause_already:              "⏸️ Hermes is already paused{suffix}. Use `/pause off` to resume."
    pause_engaged:              "⏸️ Paused{suffix}. New cron/kanban/gateway work is on hold; in-flight work finishes normally. Use `/pause off` to resume."
    queue_usage:                "Usage: /queue <prompt>"
    queue_queued:               "Queued for the next turn."
    queue_depth:                " ({depth} queued)"
    steer_usage:                "Usage: /steer <prompt>"
    steer_agent_starting:       "Agent still starting — /steer queued for the next turn."
    steer_no_agent:             "No active agent — /steer queued for the next turn."
    steer_failed:               "⚠️ Steer failed: {error}"
    steer_rejected:             "Steer rejected (empty payload)."
    steer_target_subagents:     "run and its active subagent(s)"
    steer_target_run:           "run"
    steer_queued:               "⏩ Steer queued into current {target} — arrives after the next tool call: '{preview}'"
    goal_busy:                  "Agent is running — use /goal status / pause / clear / wait mid-run, or /stop before setting a new goal."
    loop_busy:                  "Agent is running — use /loop status / pause / stop mid-run, or /stop before setting a new loop."
    access_you_can_run:         "You can run: "
    access_whoami:              ". Use /whoami for the full list."
    access_none:                "No slash commands are enabled for non-admins on this platform. Ask an admin to add you to allow_admin_from or to set user_allowed_commands."
    access_admin_only:          "⛔ /{command} is admin-only here. {suffix}"
    suggestions_failed:         "Suggestions command failed: {error}"
    blueprint_failed:           "Cron blueprint command failed: {error}"
    confirm_prompt:             "⚠️ **Confirm /{command}**\n\n{detail}\n\nChoose:\n• **Approve Once** — proceed this time only\n• **Always Approve** — proceed and silence this prompt permanently\n• **Cancel** — keep current conversation\n\n_Text fallback: reply `{prefix}approve`, `{prefix}always`, or `{prefix}cancel`._"
    confirm_cancelled:          "🟡 /{command} cancelled. Conversation unchanged."
    optout_saved:               "\n\nℹ️ Future /clear, /new, /reset, and /undo will run without confirmation. Re-enable via `approvals.destructive_slash_confirm: true` in config.yaml."
    optout_not_saved:           "\n\n⚠️ Could not save that preference (config.yaml is not writable), so /clear, /new, /reset, and /undo will ask again next time. To silence it permanently, set `approvals.destructive_slash_confirm: false` in config.yaml."
  session_activity:
    iteration:                  "iteration {count}"
    iteration_capped:           "iteration {count}/{cap}"
  run_inbound:
    update_send_failed:         "✗ Failed to send response to update process: {error}"
    update_sent:                "✓ Sent `{label}` to the update process."
    clarify_answered:           "✅ answered: {answer}"
    clarify_cancelled:          "↩️ Clarification cancelled — your message will be handled as a follow-up."
    force_stopped_pending:      "⚡ Force-stopped. The agent was still starting — session unlocked."
    hook_blocked:               "Command `/{command}` was blocked by a hook."
    confirm_new_detail:         "This starts a fresh session and discards the current conversation history."
    could_not_start:            "Could not start /{name} — please try again."
    learn_ack_described:        "Learning a skill from what you described…"
    learn_ack_conversation:     "Learning a skill from this conversation…"
    plan_ack_task:              "Planning: {task}"
    plan_ack_context:           "Planning from this conversation's context…"
    init_ack_update:            "Updating AGENTS.md from a project scan…"
    init_ack_generate:          "Generating AGENTS.md from a project scan…"
    confirm_undo_one:           "This removes the last user/assistant exchange from history."
    confirm_undo_many:          "This removes the last {count} user turns from history."
    steer_usage_idle:           "Usage: /steer <prompt>  (no agent is running; sending as a normal message)"
    moa_failed:                 "Failed to prepare MoA turn."
    quick_no_output:            "Command returned no output."
    quick_timeout:              "Quick command timed out (30s)."
    quick_error:                "Quick command error: {error}"
    drain_no_new_work:          "⏳ Gateway is {action} and is not accepting new work right now."
    quick_no_command:           "Quick command '/{command}' has no command defined."
    quick_bad_type:             "Quick command '/{command}' has unsupported type (supported: 'exec', 'alias')."
    quick_no_target:            "Quick command '/{command}' has no target defined."
    unknown_command:            "Unknown command `/{command}`. Type /commands to see what's available, or resend without the leading slash to send as a regular message."
    skill_disabled:             "The **{skill}** skill is disabled for {platform}.\nEnable it with: `hermes skills config`"
    skills_disabled:            "The **{skills}** skill(s) in this stacked invocation are disabled for {platform}.\nEnable them with: `hermes skills config`"
    stacked_failed:             "Failed to load stacked skills for /{command}."
    external_drain:             "⏳ This agent is draining for a maintenance action and isn't accepting new turns right now. It'll be back in a moment — please resend shortly."
    turn_lease_timeout:         "⏳ Another turn is still running on this session. To protect the transcript, this message was not processed. Wait for the active turn to finish, then resend it."
  run_inbound_unauthorized:
    hour_one:                   "{hours} hour"
    hour_many:                  "{hours} hours"
    pairing_code:               "Hi! I don't recognize you yet, so I can't reply until the person running this bot approves you.\n\nYour pairing code: `{code}` (valid for {validity})\n\nIf you run this bot, open a terminal and run: `{approve_cmd}`. Otherwise send that command to the bot owner. After approval, send your message again."
    pairing_rate_limited:       "Too many pairing requests right now. Wait a few minutes, then send your message again."
    allowlist_env:              "add the ID to {env_var} in {hermes_home}/.env and restart the gateway"
    allowlist_generic:          "add the ID to this platform's allowed-users list and restart the gateway"
    owner_hint:                 "Dropped a message from unrecognized {platform} user {who}. If that is you or someone you trust, {allowlist}; or set `unauthorized_dm_behavior: pair` for {platform} in {hermes_home}/config.yaml so unknown senders receive a pairing code you can approve with `hermes pairing approve {platform} <code>`."
  run_shutdown:
    cron_action_restarting:     "restarting"
    cron_action_shutting_down:  "shutting down"
    cron_interrupted:           "⚠️ Scheduled job '{job_name}' was cut short because Hermes is {action}; no result this run. It will run again on schedule, or run it now with `hermes cron run {job_name}` once Hermes is back."
    notice_shutdown:            "⚠️ Hermes is shutting down — your current task will be interrupted. When it is back online, send any message and I'll try to pick up where we left off."
    notice_restart:             "⚠️ Hermes is restarting — your current task will be interrupted. Send any message after the restart and I'll try to resume where you left off."
  run_notifications:
    update_failed:              "❌ Hermes update failed; the previous version is still running. Run `hermes update` on the host to see the full error, or try /update again later."
    update_default_hint:        " (default: {default})"
    update_prompt:              "☤ **Update needs your input:**\n\n{prompt}{default_hint}\n\nReply `{prefix}approve` (yes) or `{prefix}deny` (no), or type your answer directly."
    update_finished:            "✅ Hermes update finished."
    update_timed_out:           "❌ Hermes update timed out after 30 minutes."
    update_succeeded:           "✅ Hermes update finished successfully."
    update_last_lines:          "\n\nLast lines:\n"
    restarted:                  "♻ Gateway restarted successfully. Your session continues."
    free_tier:                  "Inference: Nous free tier (nous/welcome). Sign in for more: /login"
    gateway_online:             "♻️ Gateway online — Hermes is back and ready."
    db_corrupt:                 "⚠️ Session database corruption detected. Messages may not be persisted. Recovery options:\n1. Run `hermes {profile_arg}doctor --fix`\n2. Stop the gateway, then recover with:\n   hermes {profile_arg}sessions recover --source {db_path} --inspect-only\n   (if it reports recoverable) hermes {profile_arg}sessions recover --source {db_path} --output recovered-state.db\n   — recovery snapshots the damaged file first; do NOT run `sqlite3 ... \".recover\"` against the live state.db, a vulnerable sqlite3 CLI can corrupt it further\n3. Restore from a backup in {backups_dir}/\nRun `hermes {profile_arg}doctor` for sanitized diagnostics."
    db_fts_corrupt:             "⚠️ Session database reported a corruption error confined to the search index (FTS5); the message tables are not damaged. Messages may not be persisted until it is repaired: run `hermes {profile_arg}doctor --fix`, then restart the gateway. Do not run recovery tools or restore a backup unless `hermes {profile_arg}doctor` confirms damage."
    db_then_restart:            "{action} Then `hermes {profile_arg}gateway restart`."
    db_unavailable:             "⚠️ Session database unavailable — messages may not be saved and /resume will be empty. Cause: {cause}. {action}"
    final_output:               "\n\nFinal output:\n"
    bg_still_running:           "⏳ Background task still running"
    recent_output:              "\n\nRecent output:\n"
  delivery_ledger:
    recovered:                  "♻️ Recovered reply — the gateway restarted during delivery, so this may be a duplicate:\n\n"
    reconnected:                "♻️ Recovered reply — the messaging platform reconnected after the original delivery failed, so this may be a duplicate:\n\n"
    flood:                      "♻️ Recovered reply — the messaging platform's rate limit refused the original, so part of it may already have arrived above:\n\n"
  run_voice:
    channels_unsupported:       "Voice channels are not supported on this platform."
  run:
    hygiene_timeout: "⚠️ Shortening the conversation history took too long, so I skipped it and kept everything as-is. Run /compress to try again or /new to start fresh."
    hygiene_timeout_doctor_hint: " If this keeps happening, run `hermes doctor` on the host."
    approval_smart_deny_heading: "⚠️ **Smart DENY — owner override for one operation:**"
    approval_reply_once: "Reply `{prefix}approve` to run it once"
    approval_reply_session: "`{prefix}approve session` to allow this pattern for the rest of this session"
    approval_reply_always: "`{prefix}approve always` to allow it permanently"
    approval_reply_deny: "`{prefix}deny` to cancel"
    approval_reply_or_last: ", or {last}.\n"
    provider_rate_limited: "⏱️ The AI model service is rate-limiting requests. Wait a moment, then use /retry."
    provider_auth_failed: "⚠️ Sign-in to the AI model service failed. Use /login to sign in again, or ask whoever runs this bot to run `hermes doctor` on the host."
    provider_policy_rejected: "⚠️ The AI model service rejected this request. Try rephrasing your message, or use /model to switch models."
    provider_connection_interrupted: "⚠️ The connection to the AI model service was interrupted mid-request — usually transient. Use /retry to try again; if it keeps happening, run `hermes doctor` on the host."
    provider_endpoint_unreachable: "⚠️ The AI model service isn't reachable right now — the configured model endpoint is not running or is unreachable. Wait a moment and use /retry; if it persists, run `hermes doctor` on the host."
    provider_connection_error: "⚠️ Hermes could not reach the AI model service (no further detail from the SDK). Use /retry to try again; if it persists, run `hermes doctor` on the host."
    provider_usage_limit_resets: "⏱️ The AI model service's usage limit is reached; it resets in {window}. Use /retry after that, or /model to switch models."
    provider_kept_failing: "⚠️ The AI model service kept failing. Use /retry to try again, or /model to switch models. Details are in the gateway log (`hermes logs`)."
    context_overflow: "⚠️ This conversation has grown too long for me to read all at once. Use /compress to shorten the history, or /new to start a fresh conversation."
    busy_reject_model: "Agent is running — wait or /stop first, then switch models."
    busy_reject_codex_runtime: "Agent is running — wait or /stop first, then change runtime."
    busy_reject_moa: "Agent is running — wait or /stop first, then run /moa."
    skill_disabled: "The **{name}** skill is installed but disabled.\nEnable it with: `hermes skills config`"
    skill_not_installed: "The **{name}** skill is available but not installed.\nInstall it with: `hermes skills install {install_path}`"
    bg_process_finished: "{icon} Background task finished"
    bg_process_failed: "{icon} Background task failed"
    bg_process_exit: "exit {code}"
    bg_process_last_output: ". Last output:\n```\n{tail}\n```"
    bg_process_rerun_hint: "\nAsk me to rerun it or show the full log."
    session_storage_disk: "⚠️ Session storage was temporarily unavailable, so this turn was stopped to protect your conversation history. Please check available disk space, then send your message again."
    session_storage_unavailable: "⚠️ Session storage was temporarily unavailable, so this turn was stopped to protect your conversation history. Your message should already be saved — please send it again in a moment."
    turn_failed_generic: "⚠️ Something went wrong and I couldn't finish this reply. Use /retry to try again, or /new to start a fresh conversation. Technical details are in the gateway log (`hermes logs`)."
    interrupted_before_start: "⚠️ Your message was interrupted before processing started (likely by a recent /stop). Please send it again."
    stopped_before_finishing: "⚠️ I had to stop before finishing{reason}. Use /retry to try again, or /compress if this conversation has grown very long."
    no_response_generated: "⚠️ Processing completed but no response was generated. This may be a transient error — try sending your message again."
    not_processed_cleanup: "⚠️ Your message wasn't processed (the previous turn was still being cleaned up). Please send it again."
  run_turn:
    unexpected_silence: "⚠️ The model returned only a silence marker for a message that needed a reply. Try again or rephrase."
    auto_reset_notice: "◐ Session reset after being stopped. Conversation history cleared.\nUse /resume to browse and restore a previous session.\n"
    hygiene_failed: "⚠️ Shortening the conversation history failed, so I kept everything as-is. Run /compress to try again or /new to start fresh. If this keeps happening, run `hermes doctor` on the host."
    no_home_channel: "📬 No home channel is set for {platform}. A home channel is where Hermes delivers cron job results and cross-platform messages.\n\nType {sethome_cmd} to make this chat your home channel, or ignore to skip."
    reasoning_header_subtext: "-# 💭 Reasoning"
    reasoning_header_blockquote: "> 💭 **Reasoning:**"
    reasoning_more_lines: "\n_... ({count} more lines)_"
    reasoning_block: "💭 **Reasoning:**\n```\n{reasoning}\n```\n\n{response}"
    compression_exhausted_reset: "\n\n🔄 Session auto-reset — the conversation exceeded the maximum context size and could not be compressed further. Your next message will start a fresh session."
    status_hint_401: " Your sign-in to the AI model service has expired or the API key is wrong. Use /login here, or run `{relogin}` on the host."
    status_hint_402: " Your AI model service balance or quota is used up. Top it up on the service's website, or use /model to switch models."
    status_hint_529: " The AI model service is temporarily overloaded. Wait a moment, then use /retry."
    status_hint_rate_limited: " You are being rate-limited. Please wait a moment and try again."
    status_hint_usage_limit_resets: " Your plan's usage limit has been reached. It resets in ~{hours}h."
    status_hint_usage_limit: " Your plan's usage limit has been reached. Please wait until it resets."
    status_hint_rejected: " The AI model service rejected the request."
    agent_error: "⚠️ Something went wrong and I couldn't finish this reply.{status_hint}\nUse /retry to try again, or /new to start a fresh conversation. Technical details are in the gateway log (`hermes logs`)."
    history_unavailable: "⚠️ This session's history is temporarily unavailable, so this message was not processed. Ask the operator to inspect state.db, then resend after it is healthy. Use /reset only if you intentionally want to start a new conversation."
    ctx_source_config: "config"
    ctx_source_default: "default — set model.context_length in config to override"
    ctx_source_detected: "detected"
    info_model: "◆ Model: `{model}`"
    info_provider: "◆ Provider: {provider}"
    info_context: "◆ Context: {tokens} tokens ({source})"
    info_acting_model: "◆ Acting model (billed for the run): {provider}:{model}"
    info_endpoint: "◆ Endpoint: {url}"
    bg_no_credentials: "❌ The background task couldn't start because no AI model sign-in is configured. Use /login, or run `hermes setup` on the host."
    bg_error: "Error: {error}"
    bg_complete_header: "✅ Background task complete\nPrompt: \"{preview}\"\n\n"
    bg_no_response: "(No response generated)"
    bg_failed: "❌ Your background task \"{preview}\" failed before finishing. Send /bg again to retry, or /agents to see what is still running."
    proxy_needs_aiohttp: "⚠️ Proxy mode requires aiohttp. Run: {hint}"
    proxy_url_missing: "⚠️ Proxy URL not configured (GATEWAY_PROXY_URL or gateway.proxy_url)"
    proxy_error: "⚠️ Proxy error ({status}): {error}"
    proxy_closed: "⚠️ Proxy connection closed before the response completed"
    proxy_connection_error: "⚠️ Proxy connection error: {error}"
    proxy_no_response: "(No response from remote agent)"
    inactivity_warning: "⚠️ I seem to be stuck (no activity for {idle} min). If nothing happens in the next {remaining} min I'll give up on this task. You can keep waiting, send /stop to cancel it, or /new to start a fresh conversation."
    timeout_inactive: "⏱️ Agent inactive for {mins} min — no tool calls or API responses."
    timeout_stuck_on_tool: "The agent appears stuck on tool `{tool}` ({secs}s since last activity, {progress})."
    timeout_last_activity: "Last activity: {desc} ({secs}s ago, {progress}). The agent may have been waiting on an API response."
    timeout_hint: "To increase the limit, set agent.gateway_timeout in config.yaml (value in seconds, 0 = no limit) and restart the gateway.\nTry again, or use /reset to start fresh."
    heartbeat_working: "⏳ Working — {mins} min{detail}"
  run_turn_runner:
    task_card_title: "Hermes is working"
    task_running: "running"
    task_complete: "complete"
    task_error: "error"
    clarify_boundary_placeholder: "💬 等待你的选择..."
    clarify_expired: "⏳ This prompt expired — please send a new request."
    provider_connect_failed: "⚠️ I couldn't connect to the AI model service, so this message wasn't processed. Use /login to sign in again, or /model to pick a different model. If it keeps failing, run `hermes doctor` on the host."
  session_stall:
    stuck: "⚠️ I seem to be stuck (no activity for {mins} min). Send /stop to cancel the current task, or /new to start a fresh conversation."
  stream_consumer:
    boundary_placeholder: "⏸ 等待审批中..."
  kanban_watchers_notifier:
    completed: "✔ {head} done — {title}{handoff}"
    review_requested: "👀 {head} ready for review — {title}{handoff}"
    changes_default_reason: "reviewer feedback requires changes"
    changes_reviewer: " — reviewer @{reviewer}"
    changes_implementer: " → implementer @{implementer}"
    changes_requested: "🛑 {board_tag}Kanban {task_id} review requested changes/BLOCK: {reason}{provenance}"
    triage: "🛑 {head} routed to TRIAGE — {why}{recurrences}{reason}"
    triage_decision: "needs a human decision"
    triage_orchestration: "for orchestration attention"
    triage_recurrences: " (blocked {}x for the same cause)"
    gave_up_count: "it failed {count} times in a row"
    gave_up_kept_failing: "it kept failing"
    gave_up_last: " (last: {})"
    gave_up: "⛔ {head} is now blocked: {count}{last}. Fix the cause, then `hermes kanban unblock {task_id}` (or `hermes kanban reassign {task_id}`). Logs: `hermes kanban log {task_id}`."
    timed_out_minutes: "its {minutes}-minute limit"
    timed_out_limit: "its time limit"
    timed_out: "⏱ {head} ran past {span} and was stopped; it will be retried automatically."
    blocked: "⏸ {head} blocked{reason}"
    crashed: "✖ {head} — its worker stopped unexpectedly; it will be retried automatically."
  platform_base:
    media_kind_audio: "audio"
    media_kind_video: "video"
    media_kind_file: "file"
    media_kind_image: "image"
    media_failure: "⚠️ Couldn't deliver the {kind} attachment{suffix}."
    unauthorized_action: "This bot is private and you're not on its allowed list. If you own it, run `hermes pairing approve {platform} <request-id>` on the host (`hermes pairing list` shows the id)."
    ea_smart_deny_line: "\n\nSmart DENY: owner override applies to this one operation only."
    ea_allow_once: "Allow Once"
    ea_allow_session: "Allow Session"
    ea_always_allow: "Always Allow"
    ea_deny: "Deny"
    clarify_hint: "Reply with the number, the option text, or your own answer."
    clarify_hint_multi: "Multiple selections allowed — reply with the numbers separated by commas or spaces (e.g. \"1, 3\"), the option text, or your own answer."
    delivery_failed: "⚠️ Message delivery failed after multiple attempts. Please try again — your request was processed but the response could not be sent."
    formatting_failed: "(Response formatting failed, plain text:)\n\n{content}"
    turn_error_no_details: "no details available"
    turn_error: "Sorry, I encountered an error ({error_type}).\n{detail}\nTry again or use /reset to start a fresh session."
    turn_error_short: "Sorry, I encountered an error."
  platform_base_exec_approval:
    header_text: "Hermes wants to run a command that needs your OK"
    reason_label_text: "Why it was flagged"
    deadline_line: "If you don't answer within {window} it will NOT run."
    timed_out_notice: "⌛ Approval timed out after {window} — the command was NOT run. Ask me to try again if you still want it, or raise approvals.timeout in config.yaml."
    window_second_one: "{count} second"
    window_second_few: "{count} seconds"
    window_second_many: "{count} seconds"
    window_minute_one: "{count} minute"
    window_minute_few: "{count} minutes"
    window_minute_many: "{count} minutes"
    window_hour_one: "{count} hour"
    window_hour_few: "{count} hours"
    window_hour_many: "{count} hours"
  slash_commands:
    busy_queue_status: "queues for next turn"
    busy_queue_set: "Messages will be queued for the next turn while Hermes is busy."
    busy_steer_status: "steers into current run (after next tool call)"
    busy_steer_set: "Messages will be steered into the current run (after the next tool call)."
    busy_interrupt_status: "interrupts current run"
    busy_interrupt_set: "Messages will interrupt the current run while Hermes is busy."
    busy_status: "**Busy input mode: `{mode}`\nMessages while busy: _{behavior}_\nChange with `/busy queue`, `/busy steer`, or `/busy interrupt`."
    busy_unknown: "Unknown mode `{arg}`. Use `/busy queue`, `/busy steer`, or `/busy interrupt`."
    busy_save_failed: "Busy input mode could not be saved to config. Mode unchanged."
    busy_set: "Busy input mode set to **`{mode}`** (saved).\n_{behavior}_"
    platform_usage: "Usage: /platform <list|pause|resume> [name]\n  /platform list — show platform status\n  /platform pause <name> — stop retrying a failing platform\n  /platform resume <name> — re-queue a paused platform"
    whoami_scope_dm: "DM"
    whoami_scope_group: "group/channel"
    whoami_head: "**You** — {platform} ({scope})\nUser ID: `{user_id}`\n"
    whoami_unrestricted: "Tier: unrestricted (no admin list configured for this scope)\nSlash commands: all available"
    whoami_admin: "Tier: **admin**\nSlash commands: all available"
    whoami_user: "Tier: user\nSlash commands you can run: {commands}"
    none: "(none)"
    platform_header: "**Gateway platforms**"
    platform_connected: "Connected: {platforms}"
    platform_paused_line: "  · {platform} — PAUSED ({reason}). Resume with `/platform resume {platform}`."
    platform_retrying_line: "  · {platform} — retrying (attempt {attempts})"
    platform_none_failed: "Failed/paused: (none)"
    platform_action_usage: "Usage: /platform {action} <name>"
    platform_unknown: "Unknown platform: {target}"
    platform_not_queued: "{name} is not in the retry queue (it's either connected or not enabled)."
    platform_already_paused: "{name} is already paused."
    platform_paused: "✓ {name} paused. Resume with `/platform resume {name}` or `hermes gateway restart` to reset."
    platform_nothing_to_resume: "{name} is not in the retry queue — nothing to resume."
    platform_already_retrying: "{name} is already retrying — no resume needed."
    platform_resumed: "✓ {name} resumed — retrying on next watcher tick."
    diff_failed_default: "Could not generate diff"
    diff_more: "\n... and {count} more"
    diff_untracked: "**Untracked:**\n```\n{shown}{more}\n```"
    diff_truncated: "\n... (truncated — {count} lines total; use /diff --stat for a summary)"
    memory_unknown: "Unknown /memory subcommand. Use: pending, approve <id>, reject <id>, approval <on|off>."
    skills_gate_off: "Skill write approval is off (skills.write_approval). Enable it with /skills approval on, then review staged writes here with /skills pending."
    skills_unknown: "Unknown /skills subcommand on this platform. Use: pending, approve <id>, reject <id>, diff <id>, approval <on|off>. (Search/install are CLI-only.)"
    skills_diff_truncated: "\n… (truncated — full diff in ~/.hermes/pending/skills/{pending_id}.json)"
    approvals_admin_only: "Only gateway admins can change the persistent approval mode."
    bundles_none: "No skill bundles installed.\nCreate one on the host with:\n  `hermes bundles create <name> --skill <s1> --skill <s2>`\nDirectory: `{dir}`"
    bundles_header: "**Skill Bundles** ({count} installed):"
    bundle_default_desc: "Load {count} skills"
    bundle_item: "• `/{slug}` — {desc} _({count} skills)_"
    bundles_footer: "Invoke a bundle with `/<slug>` to load all its skills."
    update_not_applicable: "✗ `hermes update` does not apply to this install ({method}).\nUpdate with: {command}"
  slash_commands_goals:
    gate_admin_only: "⛔ /goal gate add requires an explicitly configured gateway admin (allow_admin_from for DMs, group_allow_admin_from for groups)."
    heartbeat_unavailable: "Heartbeats unavailable (no session)."
    heartbeat_paused: "⏸ Heartbeat paused: {prompt}"
    heartbeat_none: "No heartbeat set."
    heartbeat_no_resume: "No heartbeat to resume."
    heartbeat_resumed: "▶ Heartbeat resumed (every {interval}): {prompt}"
    heartbeat_cleared: "✓ Heartbeat cleared."
    heartbeat_usage: "Usage: /heartbeat every <interval> <prompt>  (e.g. /heartbeat every 10m Check CI)\nAlso: /heartbeat status | pause | resume | clear"
    heartbeat_interval_too_small: "Interval too small — minimum is {seconds}s."
    heartbeat_prompt_required: "Usage: /heartbeat every <interval> <prompt> — the prompt is required."
    heartbeat_invalid: "Invalid heartbeat"
    heartbeat_set: "♥ Heartbeat set (every {interval}): {prompt}\nFires as a normal turn whenever this session is idle and the interval has elapsed. Lives while the gateway runs — use `hermes cron` for durable schedules."
    verb_unavailable: "{verb} unavailable (no session)."
    agent_running: "Agent is running — wait for the turn to finish, then /{verb}."
    nothing_yet: "Nothing to {verb} yet — send a message first."
    refine_empty: "Nothing to refine yet — the conversation is empty."
    refine_failed: "/refine failed to start: {error}"
    refine_focus: " (focus: {focus})"
    refine_started: "⚗ Reviewing this conversation in the background{tail} — any memory/skill updates will be reported when done."
    review_failed: "/review failed to start: {error}"
    subgoal_no_goal: "No active goal. Set one with /goal <text>."
    subgoal_remove_usage: "Usage: /subgoal remove <n>"
    subgoal_remove_invalid: "/subgoal remove: <n> must be an integer (1-based index)."
    subgoal_removed: "✓ Removed subgoal {index}: {subgoal}"
    subgoals_cleared_one: "✓ Cleared {count} subgoal."
    subgoals_cleared_many: "✓ Cleared {count} subgoals."
    subgoals_none: "No subgoals to clear."
    subgoal_added: "✓ Added subgoal {index}: {subgoal}"
    loops_unavailable: "Loops unavailable."
    loops_no_session: "Loops unavailable (no active session)."
    loop_goal_note: "\nNote: an active /goal is driving this session — loop wakeups defer until the goal finishes, pauses, or parks."
  slash_commands_model:
    code_skew: "This gateway is running code from {boot_rev} but the checkout on disk is now {disk_rev}. Switching models would risk a stale-module crash — restart the gateway to load the new code: hermes gateway restart"
    switch_failed: "Model switch to {model} failed ({error}); staying on {current}."
    config_not_updated: "config.yaml not updated ({error})"
    stale_override_not_cleared: "saved to config.yaml, but the stale session override was not cleared ({error})"
    next_turn_only: "    (next turn only — restores after one response)"
    switch_cancelled: "🟡 Model switch cancelled. Current model unchanged ({model})."
    guard_prompt: "⚠️ **{title}**\n\n{message}\n\n_Text fallback: reply `{prefix}approve` to switch or `{prefix}cancel` to keep the current model._"
    config_load_failed: "❌ Could not load config: {error}"
    config_write_failed: "config write failed"
  slash_commands_session:
    unknown_error: "unknown error"
    retry_unsafe: "Cannot retry that message safely: {error}"
    retry_failed: "Retry failed; transcript was not changed."
    codex_nothing_to_compact: "🗜️ Nothing to compact: this session runs on the Codex app-server runtime, whose context lives in a Codex-owned thread that only exists while the agent is active. Send a message first, then /compress — or /reset to start fresh."
    codex_compacted: "🗜️ Codex app-server thread compacted (thread/compact). The transcript mirror is unchanged by design — the app-server now carries the compacted context."
    codex_compact_incomplete: "⚠️ Codex app-server compaction did not complete — the thread is unchanged. Check the app-server logs, retry /compress, or /reset for a clean session."
    save_no_messages: "No stored messages found for this session ({session_id})."
    save_no_adapter: "Platform adapter not found to send the document."
    save_caption: "Session export: {filename}"
    save_done: "Export complete."
    save_failed: "Error exporting session: {error}"
    sessions_search_usage: "Usage: `/sessions search <query>`"
    sessions_all_requires_admin: "_Note: `all` (cross-chat listing) requires a configured admin; showing this chat's sessions only._"
    sessions_matching: "Sessions matching “{query}”"
    sessions_title: "Sessions"
    sessions_named_title: "Named Sessions"
  slash_commands_status:
    history_unreadable: "⚠️ I can't read this conversation's history right now (your earlier messages exist but cannot be loaded). Run `hermes doctor --fix` on the host, or use /new to start fresh."
    topup_header: "💳 **Nous balance**"
    topup_portal: "Manage billing on the portal: {url}"
    topup_hint: "Top up and manage billing in the browser — your balance updates here after."
    deleg_no_progress: " · no progress {seconds}s"
    deleg_quiet: " · quiet {seconds}s"
    deleg_between_turns: "between turns"
    deleg_child: "  - child {index}: {calls} api calls · {doing}"
    deleg_active_ago: " · active {seconds}s ago"
  slack:
    # Slack adapter (plugins/platforms/slack/adapter.py) -- fork-only keys, en + pl.
    model_picker_expired:         "⏳ This model picker expired — please run /model again."
    reply_truncated:              "\n\n_[Reply truncated: {dropped} more part(s) exceeded Slack's ephemeral reply limit.]_"
    slash_running:                "Running `/{slash}`…"
    handoff_seed:                 ":thread: Hermes handoff — *{name}*"
    status_thinking:              "is thinking..."
    status_still_working:         "still working… ({elapsed})"
    feedback_good:                "Good Response"
    feedback_good_a11y:           "Submit positive feedback on this response"
    feedback_bad:                 "Bad Response"
    feedback_bad_a11y:            "Submit negative feedback on this response"
    image_undelivered:            "⚠️ Couldn't deliver the image attachment."
    video_undelivered:            "⚠️ Couldn't deliver the video attachment."
    file_undelivered:             "⚠️ Couldn't deliver the file attachment ({name})."
    ea_smart_deny_line:           "\n*Smart DENY:* owner override applies to this one operation only."
    approval_required_fallback:   "⚠️ Command approval required: {command}"
    confirm_title:                "Confirm"
    button_approve_once:          "Approve Once"
    button_always_approve:        "Always Approve"
    button_cancel:                "Cancel"
    picker_provider_option:       "{name} ({count} models)"
    picker_more_available:        "\n*{count} more available — type `/model <name>` directly*"
    picker_provider_section:      "*⚙ Model Configuration*\nCurrent model: `{model}`\nProvider: {provider}\n\nSelect a provider:{extra}"
    picker_unknown_model:         "unknown"
    picker_choose_provider:       "Choose a provider…"
    picker_model_section:         "*⚙ Model Configuration*\n\nProvider: *{provider}*\nSelect a model:{extra}"
    picker_choose_model:          "Choose a model from {provider}…"
    picker_back:                  "◀ Back"
    picker_select_provider_fallback: "⚙ Model Configuration — select a provider"
    picker_provider_fallback:     "⚙ Model Configuration — {provider}"
    picker_cancelled:             "❌ Model selection cancelled."
    picker_no_models:             "No models available for `{provider}`."
    picker_switching:             "⚙ Switching to `{model}`…"
    picker_switch_failed:         "❌ Model switch failed: {error}"
    picker_header_failed:         "⚙ Model Switch Failed"
    picker_header_switched:       "⚙ Model Switched"
    clarify_option:               "Option {n}"
    clarify_other:                "✏️ Other…"
    clarify_expired_by:           "⏳ This prompt expired — please send a new request. (by {user})"
    clarify_awaiting_typed:       "✏️ Awaiting typed answer from {user}…"
    approved_once_by:             "✅ Approved once by {user}"
    approved_session_by:          "✅ Approved for session by {user}"
    approved_always_by:           "✅ Approved permanently by {user}"
    denied_by:                    "❌ Denied by {user}"
    always_approved_by:           "🔒 Always approved by {user}"
    cancelled_by:                 "❌ Cancelled by {user}"
    approval_expired:             "⌛ Approval expired — command was not run (already timed out or resolved elsewhere)"
    placeholder_confirmation:     "Confirmation prompt"
    placeholder_approval:         "Command approval request"
    placeholder_clarify:          "Clarification"
  display:
    # Slack live status after the bot's name ("Seba is reading …"); only used for non-English.
    status_using: "is using {tool}"
    status_connector_for: " for "
    status_verb:
      web_search: "is searching the web"
      web_extract: "is reading"
      browser_navigate: "is browsing"
      browser_click: "is clicking"
      browser_type: "is typing"
      read_file: "is reading"
      write_file: "is writing"
      patch: "is editing"
      search_files: "is searching files"
      terminal: "is running"
      execute_code: "is running code"
      image_generate: "is generating image"
      video_generate: "is generating video"
      text_to_speech: "is generating speech"
      vision_analyze: "is looking at the image"
      session_search: "is searching past sessions"
      skill_view: "is reading skill"
      skills_list: "is listing skills"
      skill_manage: "is updating skill"
      delegate_task: "is delegating"
      cronjob_manage: "is scheduling"
      clarify: "is asking"
      memory: "is updating memory"
      todo_list: "is updating tasks"
